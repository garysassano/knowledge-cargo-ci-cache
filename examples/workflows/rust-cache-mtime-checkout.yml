name: Conditional Whole Target Rust Cache

on:
  workflow_dispatch:
  push:
    paths:
      - app/**

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      CARGO_INCREMENTAL: "0"
      LOCAL_WORKSPACE: /tmp/example-workspace
      LOCAL_CARGO_TARGET_DIR: /tmp/example-cargo-target

    defaults:
      run:
        working-directory: /tmp/example-workspace/app

    steps:
      - name: Checkout workflow actions
        uses: actions/checkout@v7
        with:
          sparse-checkout: |
            examples/actions/cached-worktree-checkout

      - name: Restore cached worktree
        uses: actions/cache@v6
        with:
          path: ${{ env.LOCAL_WORKSPACE }}
          key: workspace-${{ runner.os }}-${{ runner.arch }}-${{ github.ref_name }}-${{ github.sha }}
          restore-keys: |
            workspace-${{ runner.os }}-${{ runner.arch }}-${{ github.ref_name }}-

      - name: Checkout cached worktree
        uses: ./examples/actions/cached-worktree-checkout
        with:
          path: ${{ env.LOCAL_WORKSPACE }}
          sparse-paths: app

      - name: Configure Cargo target
        run: mkdir -p "$LOCAL_CARGO_TARGET_DIR"

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust deps and target
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: ${{ env.LOCAL_WORKSPACE }}/app -> ../../example-cargo-target
          # Explicit although true by default: include the configured target directory.
          cache-targets: true
          # Also retain matching target artifacts for workspace members such as crates/common.
          cache-workspace-crates: true
          cache-bin: false
          cache-all-crates: false
          # Put exact source and build-command identity in the fallback lineage.
          shared-key: example-target-release-locked-v2-${{ github.sha }}
          # Adapt the branch name if the repository's trusted default branch is not main.
          save-if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}

      - name: Build
        env:
          CARGO_TARGET_DIR: ${{ env.LOCAL_CARGO_TARGET_DIR }}
        run: cargo build --workspace --release --locked
